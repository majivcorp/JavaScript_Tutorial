// Commented out section was my process. - Just for future review and reflection
//function my_each(param) {
    
    //index = 0;
    //var len = param.length;
    //var str = "";

    //while (index < len) {
        //return str = param[index];
        //index++
    ///}
//}
//console.log(my_each(["Banana", "Orange", "Apple", "Mango"]));

function my_each(param) {
    for (i = 0; i < param.length; i++) {
        console.log(param[i]);
    } 
}

//console.log(["blah1", "blah2", "blah3"]);